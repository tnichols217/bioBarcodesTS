{
	"name": "bercode-generator",
	"version": "0.0.1",
	"description": "Generates barcodes form inventory files for the Dalat Biology laboratory",
	"main": "build/src/main.js",
	"scripts": {
		"transpile": "npx tsc",
		"transpile-watch": "npx tsc -w",
		"bundle-dev": "node src/esbuild.config.mjs",
		"bundle-watch": "node src/esbuild.config.mjs watch",
		"bundle": "node src/esbuild.config.mjs production",
		"build-dev": "npx tsc && node src/esbuild.config.mjs",
		"build": "npx tsc && node src/esbuild.config.mjs production",
		"mon": "nodemon -x \"npm run transpile && node out/build/src/main.js\" -w src",
		"mon-bundle": "nodemon -x \"npm run transpile && npm run bundle-dev && node out/bundle/main.js\" -w src"
	},
	"keywords": [],
	"author": "Trevor Nichols",
	"license": "GPL-3.0-or-later",
	"devDependencies": {
		"esbuild": "^0.15.5",
		"typescript": "^4.8.2"
	}
}
